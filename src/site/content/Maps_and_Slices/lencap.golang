x := make([]int64, 0)
fmt.Printf("len: %v - cap: %v", len(x), cap(x)) // len: 0 - cap: 0

x = append(x, 5)
fmt.Printf("len: %v - cap: %v", len(x), cap(x)) // len: 1 - cap: 1

x = append(x, 10, 15)
fmt.Printf("len: %v - cap: %v", len(x), cap(x)) // len: 3 - cap: 4